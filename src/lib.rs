//! Terminal-paste markdown cleaner. See spec.md.
//! v1: deterministic-only. Public surface is `clean`.

/// Stub — implemented via TDD. Returns empty so tests fail first (RED).
pub fn clean(_input: &str) -> String {
    String::new()
}
